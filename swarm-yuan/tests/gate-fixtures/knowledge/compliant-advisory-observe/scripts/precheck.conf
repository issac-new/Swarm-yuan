# knowledge fixture (compliant-advisory-observe)：项目有 AGENTS.md 但生成的 SKILL.md 未引用任何知识来源 → advisory warn（WP-Q2H-B 转 advisory 档）
# 注：fixture 内 .claude/ 目录使 has_memories 恒为 1（与宿主机 $HOME 解耦），refs=0 时 fail 确定
PROJECT_DIR="__REPO_ROOT__"
