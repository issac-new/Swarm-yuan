# frontend fixture (compliant)：浅嵌套 + 少量 props + 无循环依赖 → pass
# madge mock：conf 注入空输出函数（无 circular 字样），确定性表达「madge 可用且未检出循环依赖」，
# 避免真 madge 有无/输出格式差异导致的跨机不确定性；门禁判定语义与输出行不变。
PROJECT_DIR="__REPO_ROOT__"
COMPONENT_DIR="src/components"
MAX_COMPONENT_DEPTH=3
MAX_PROPS_COUNT=5
madge() {
  return 0
}
