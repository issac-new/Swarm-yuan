# compliance violating-map：STANDARDS_MAP_FILE 指向格式破损的映射表 → 双 fail
# 矩阵锚点核验走 fixture 自带合规矩阵（与既有 violating/compliant 同构），
# 确保 fail 只来自映射表核验、不来自矩阵核验的意外失败
PROJECT_DIR="__REPO_ROOT__"
COMPLIANCE_MATRIX_FILE="__REPO_ROOT__/references/standards-compliance.md"
STANDARDS_MAP_FILE="standards-map-broken.conf"
