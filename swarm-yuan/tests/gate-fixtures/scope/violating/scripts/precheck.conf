# scope fixture (violating)：feat 分支改动了只读目录 upstream/（git diff main...HEAD 命中 READONLY_DIRS）
# → fail「只读目录有改动: upstream」
# git 仓库由 scripts/setup.sh 运行时生成，teardown.sh 清理
PROJECT_DIR="__REPO_ROOT__"
WRITABLE_DIRS=("src")
READONLY_DIRS=("upstream")
