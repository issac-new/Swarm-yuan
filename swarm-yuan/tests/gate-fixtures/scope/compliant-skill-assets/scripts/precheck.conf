# scope fixture (compliant-skill-assets，R23 回归 D7)：feat 分支提交 .claude/skills/ 与 .swarm-yuan/ 账本变更（工具链自有路径）→ 不算只读违规 → pass
PROJECT_DIR="__REPO_ROOT__"
WRITABLE_DIRS=("src")
READONLY_DIRS=(".claude/skills" ".swarm-yuan")
