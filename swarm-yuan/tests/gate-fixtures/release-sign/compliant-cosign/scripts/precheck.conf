# release-sign fixture (compliant-cosign)：app.zip.sig 齐备 + mock cosign 验签成功 → pass
# （验证 cosign verify-blob 正常路径；PATH 注入走 conf，setup.sh 生成 bin/cosign mock）
PROJECT_DIR="__REPO_ROOT__"
export PATH="__REPO_ROOT__/bin:$PATH"
RELEASE_SIGN_REQUIRED=1
