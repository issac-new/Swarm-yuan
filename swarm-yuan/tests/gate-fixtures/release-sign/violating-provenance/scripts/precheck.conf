# release-sign fixture (violating-provenance)：签名齐备但 RELEASE_PROVENANCE_REQUIRED=1
# 且 dist/provenance.json 不存在 → gate_release_provenance_missing fail（fail-closed）
PROJECT_DIR="__REPO_ROOT__"
RELEASE_SIGN_REQUIRED=1
RELEASE_SIGN_TOOL="none"
RELEASE_PROVENANCE_REQUIRED=1
