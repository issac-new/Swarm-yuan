# release-sign fixture (violating-cosign-fail)：app.zip.sig 齐备但 mock cosign 验签返回失败
# → gate_release_sign_verify_failed:dist/app.zip fail
# PATH 注入走 conf；setup.sh 生成 bin/cosign mock（同 sensitive gitleaks 模式）。
PROJECT_DIR="__REPO_ROOT__"
export PATH="__REPO_ROOT__/bin:$PATH"
RELEASE_SIGN_REQUIRED=1
