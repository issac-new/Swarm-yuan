# deps fixture (violating)：package.json 将 lodash 升到 4.18.0（基线 4.17.21），
# 且不设 SPEC_FILE（fixture 内也无 .claude/skills 可回退发现 spec）→ fail「依赖版本被变更但未声明」
PROJECT_DIR="__REPO_ROOT__"
CODEBASE_REF="__REPO_ROOT__/references/codebase.md"
