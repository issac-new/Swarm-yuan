# layer fixture (violating)：domain 层向上 import app 层（层穿透）+ 领域层 import express（框架污染）→ 双 fail
PROJECT_DIR="__REPO_ROOT__"
LAYER_DEFS=("app=src/app/**" "domain=src/domain/**" "infra=src/infra/**")
LAYER_ORDER=("app" "domain" "infra")
DOMAIN_LAYER="domain"
