# requirements-openspec fixture (violating)：bin/openspec 为 fixture double，经 PATH 前置注入，
# 模拟 openspec validate --all --strict 输出含 "1 failed" → gate_requirements_openspec_invalid fail
PROJECT_DIR="__REPO_ROOT__"
OPENSPEC_SPEC_DIR="openspec"
PATH="__REPO_ROOT__/bin:$PATH"
