# requirements fixture (violating)：STRICT=1 且 spec 含 TBD → gate_requirements_tbd:<line> fail；
# ID_REQUIRED=1 且需求列表项缺 REQ- 编号 → gate_requirements_no_id fail；exit 非 0
PROJECT_DIR="__REPO_ROOT__"
SPEC_FILE="docs/spec.md"
REQUIREMENTS_STRICT=1
REQUIREMENTS_ID_REQUIRED=1
