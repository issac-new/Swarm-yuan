# requirements-openspec fixture (compliant)：bin/openspec 为 fixture double，经 PATH 前置注入，
# 模拟 openspec validate --all --strict 全部通过 → pass「openspec validate 通过」
PROJECT_DIR="__REPO_ROOT__"
OPENSPEC_SPEC_DIR="openspec"
PATH="__REPO_ROOT__/bin:$PATH"
