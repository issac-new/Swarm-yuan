# summary fixture (compliant)：最小 conf 跑 --all-full，验证执行汇总披露行（跳过计数可见），退出码 0
# BRANCH_REGEX 放宽 + 保护分支置空：使本 fixture 与宿仓库当前分支名解耦，任意环境可复现
PROJECT_DIR="__REPO_ROOT__"
BRANCH_REGEX='.*'
PROTECTED_BRANCHES=()
# check_impact 硬门禁：供给含「影响范围」段的 spec（IMPACT_SPEC_FILE 专用，不触发其他 spec 消费门禁）
IMPACT_SPEC_FILE="__REPO_ROOT__/docs/impact-spec.md"
