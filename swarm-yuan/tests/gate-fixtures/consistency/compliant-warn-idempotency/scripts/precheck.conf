# consistency fixture (compliant-warn-idempotency)：写入逻辑 >5 处 → 触发幂等性 warn
# 语义说明：check_consistency 无 fail() 路径（永不 fail），warn 态属「违规语义、退出 0」，
# 故以 compliant-* 命名承载，用 expect-output 断言 warn 文案（见同目录 README.md）
PROJECT_DIR="__REPO_ROOT__"
CONSISTENCY_DIRS=("src")
