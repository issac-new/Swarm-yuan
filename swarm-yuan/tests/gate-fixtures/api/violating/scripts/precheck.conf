# api fixture (violating)：OpenAPI 定义缺 version → fail（契约无版本化）；
# 写 handler 无幂等键 + 服务无 outbox → warn 佐证（退出码由 fail 决定）
PROJECT_DIR="__REPO_ROOT__"
API_SPEC_DIR="apispecs"
WRITE_HANDLER_DIRS=("services/order-service/src")
SERVICE_DIRS=("services/order-service")
