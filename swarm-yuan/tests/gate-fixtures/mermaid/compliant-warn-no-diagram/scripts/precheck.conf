# mermaid fixture (compliant-warn-no-diagram)：reference-manual.md 无 mermaid 图 → warn，exit 0
# 语义说明：check_mermaid 无 fail() 路径（永不 fail），缺图仅 warn，
# 故 warn 态以 compliant-* 命名承载，expect-output 断言 warn 文案（见同目录 README.md）
PROJECT_DIR="__REPO_ROOT__"
