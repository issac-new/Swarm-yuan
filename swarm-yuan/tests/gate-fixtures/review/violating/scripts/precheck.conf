# review fixture (violating)：bin/ocr 为 fixture double，经 PATH 前置注入（压过宿主机真实 ocr），
# 其 review 输出含 High 级发现 → fail「ocr review 检测到 High/Critical 级问题（须修复）」
# git 仓库由 scripts/setup.sh 运行时生成（main 基线 + feat 提交，供 ocr review --from/--to 取 diff），
# teardown.sh 清理
PROJECT_DIR="__REPO_ROOT__"
PATH="__REPO_ROOT__/bin:$PATH"
WRITABLE_DIRS=("src")
