# review fixture (compliant)：bin/ocr 为 fixture double，经 PATH 前置注入（压过宿主机真实 ocr），
# 其 review 输出零发现 → pass「代码审查检查完成」
# git 仓库由 scripts/setup.sh 运行时生成，teardown.sh 清理
PROJECT_DIR="__REPO_ROOT__"
PATH="__REPO_ROOT__/bin:$PATH"
WRITABLE_DIRS=("src")
