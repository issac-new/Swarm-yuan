# review fixture (compliant-no-ocr)：PATH 收窄为系统目录以遮蔽真实 ocr（本 fixture 不带 bin/ocr），
# → 走「ocr 未安装」人工核对披露路径（warn + CRITICAL/INFORMATIONAL 两遍清单提示），退出码仍为 0
# 判定语义登记：无 ocr 时本门禁永不 fail，属纯人工核对项；fail 仅发生在 ocr review 检出 High/Critical 时
PROJECT_DIR="__REPO_ROOT__"
PATH="/usr/bin:/bin"
