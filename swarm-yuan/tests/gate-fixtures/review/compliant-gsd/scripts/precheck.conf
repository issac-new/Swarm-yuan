# review-gsd fixture (compliant-gsd)：bin/gsd-tools 为 fixture double，经 PATH 前置注入，
# 模拟 gsd-tools validate health 输出 status:healthy → pass；.planning/ 目录存在触发 gsd 守卫
PROJECT_DIR="__REPO_ROOT__"
PATH="__REPO_ROOT__/bin:$PATH"
GSD_PROJECT_DIR="__REPO_ROOT__"
WRITABLE_DIRS=("src")
