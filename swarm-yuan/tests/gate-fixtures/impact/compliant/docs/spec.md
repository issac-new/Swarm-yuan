# 订单导出功能变更规格（合规样本：含影响范围段）

## 背景

运营侧需要按日导出订单数据，当前手工跑 SQL 效率低。

## 影响范围

- 受影响系统：订单服务（新增导出模块）、对象存储（新增 bucket 目录）
- 受影响消费方：运营后台、数据平台（下游报表口径不变，仅新增订阅 topic）
- 迁移路径：灰度发布两周，先内部运营白名单再全量

## 目标

- 新增订单导出接口，支持 CSV 格式
- 导出任务异步执行，完成后站内通知

## 回滚预案

功能开关关闭即可回滚，不涉及数据结构变更。
