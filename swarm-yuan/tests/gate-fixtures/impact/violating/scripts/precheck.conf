# impact fixture (violating)：spec 不含「影响范围/消费方」段 → fail（TOGAF 变更须先声明影响分析）
# IMPACT_SPEC_FILE 显式定位 spec，不触发 WRITABLE_DIRS/SCAN_DIRS 兜底搜索，跨机确定
PROJECT_DIR="__REPO_ROOT__"
IMPACT_SPEC_FILE="docs/spec.md"
