# branch fixture (violating-on-main)：HEAD 停在保护分支 main → fail「绝不允许在保护分支 main 上开发」
# git 仓库由 scripts/setup.sh 运行时生成（git init + 基线提交后留在 main），teardown.sh 清理
PROJECT_DIR="__REPO_ROOT__"
PROTECTED_BRANCHES=("main")
BRANCH_REGEX='^(feat|fix|refactor)/.+'
