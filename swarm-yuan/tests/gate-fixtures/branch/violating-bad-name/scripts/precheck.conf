# branch fixture (violating-bad-name)：分支名 hotfix-1 不匹配 BRANCH_REGEX（且非保护分支）→ fail「分支名不规范」
# git 仓库由 scripts/setup.sh 运行时生成，teardown.sh 清理
PROJECT_DIR="__REPO_ROOT__"
PROTECTED_BRANCHES=("main")
BRANCH_REGEX='^(feat|fix|refactor)/.+'
