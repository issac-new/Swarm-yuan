# cognition fixture (compliant-full)：认知资产齐全 → 第一层 14/14 满分确定性输出（exit 0）
# 语义说明：check_cognition 永不 fail；spec 故意保持最小（无 §14/§15/§1.1/§16 段），
# 使第二~五层得分恒为 0，五层总分恒为 14/22（见同目录 README.md）
PROJECT_DIR="__REPO_ROOT__"
GLOSSARY_FILE="docs/glossary.md"
WRITABLE_DIRS=("src")
LAYER_DEFS=("app=src/app" "domain=src/domain")
LAYER_ORDER=("app" "domain")
AGGREGATE_DIR="src/domain"
CONTEXT_DIRS=("src/app" "src/domain")
SERVICE_DIRS=("src/app")
COMPONENT_DIR="src/web"
STORE_DIR="src/web/store"
SOR_FILE="docs/data-ownership.md"
SPEC_FILE="docs/spec.md"
ADR_DIR="docs/adr"
TECH_DEBT_FILE="docs/tech-debt.md"
API_SPEC_DIR="docs/api"
