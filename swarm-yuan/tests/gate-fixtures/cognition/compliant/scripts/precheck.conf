# cognition fixture (compliant)：最小认知资产 → 低分确定性输出（永不 fail，exit 0）
# 语义说明：check_cognition 无 fail() 路径，计分不足仅 warn，
# 本 fixture 断言各阶计分行与总结文案（见同目录 README.md）
PROJECT_DIR="__REPO_ROOT__"
GLOSSARY_FILE="docs/glossary.md"
WRITABLE_DIRS=("src")
LAYER_DEFS=("app=src/app" "domain=src/domain")
LAYER_ORDER=("app" "domain")
