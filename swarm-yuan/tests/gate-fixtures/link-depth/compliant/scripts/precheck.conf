# link-depth fixture (compliant)：浅调用链（entry→service 一跳）→ pass
# 注：check_link_depth 现语义为 warn-only（超深/纯转发仅 warn，永不退非 0），
#     本 fixture 断言其 pass 路径；gitnexus/graphify 需索引文件、fixture 内不触发，走兜底统计
PROJECT_DIR="__REPO_ROOT__"
MAX_LINK_DEPTH=2
WRITABLE_DIRS=("src")
