# contract fixture (violating)：order-api.yaml 缺 version 字段 + order 上下文绕过 ACL 直接 import payment 上下文 → 双 fail
PROJECT_DIR="__REPO_ROOT__"
CONTRACT_DIR="docs/contracts"
ACL_DIR="acl"
CONTEXT_DIRS=("contexts/order" "contexts/payment")
