# rtm fixture (violating-matrix-missing)：RTM_MATRIX_REQUIRED=1 但 docs/rtm.md 不存在
# → gate_rtm_matrix_missing fail（fail-closed 锚点，fail 后即返回，不级联检查 REQ）
PROJECT_DIR="__REPO_ROOT__"
SPEC_FILE="docs/spec.md"
RTM_REQUIRED=1
RTM_MATRIX_REQUIRED=1
TEST_DIR_PATTERNS=("tests/**/*.test.ts")
