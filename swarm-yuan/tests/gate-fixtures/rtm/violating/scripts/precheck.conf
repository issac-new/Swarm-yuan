# rtm fixture (violating)：RTM_REQUIRED=1 + 矩阵强制存在；spec 3 个 REQ，
# REQ-001 经测试目录追溯、REQ-002 经矩阵追溯、REQ-003 两源均无 → gate_rtm_untraced:REQ-003 fail
PROJECT_DIR="__REPO_ROOT__"
SPEC_FILE="docs/spec.md"
RTM_REQUIRED=1
RTM_MATRIX_REQUIRED=1
TEST_DIR_PATTERNS=("tests/**/*.test.ts")
