# adr fixture (compliant)：docs/adr 下 2 份 ADR（技术栈选型 + 关键架构决策）→ pass
# .git 由 scripts/setup.sh 运行时初始化（不入库）：隔离宿仓库 git diff，
# 使 §3「新增第三方依赖须有 ADR」检测输入确定（空 diff，无 warn 噪音）；teardown.sh 移除 .git
PROJECT_DIR="__REPO_ROOT__"
ADR_DIR="docs/adr"
