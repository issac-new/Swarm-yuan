# sast-deep fixture (violating-builtin)：SAST_DEEP_TOOL=builtin 强制内置词法模式族，
# 源码含 Runtime.getRuntime().exec 高危 sink → gate_sast_deep_builtin fail，exit 非 0
PROJECT_DIR="__REPO_ROOT__"
SECURITY_SCAN_DIRS=("src")
SAST_DEEP_TOOL="builtin"
SAST_DEEP_SEVERITY="error"
