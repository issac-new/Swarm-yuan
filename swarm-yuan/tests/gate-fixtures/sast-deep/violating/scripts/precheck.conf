# sast-deep fixture (violating)：SAST_DEEP_TOOL 指向 setup.sh 生成的 mock semgrep
#（输出含 1 条 ERROR severity 结果的 JSON）→ gate_sast_deep_findings fail，exit 非 0
PROJECT_DIR="__REPO_ROOT__"
SECURITY_SCAN_DIRS=("src")
SAST_DEEP_TOOL="__REPO_ROOT__/.mockbin/semgrep"
SAST_DEEP_SEVERITY="error"
