# shift-left fixture (compliant)：spec 含 §19（19.2 用例骨架 5 行）+ §21 + 回滚预案声明，
# plan 含 §20 → 三个硬门禁均 pass，exit 0
# 注：1b git diff 测试先行检查依赖宿主仓库状态（warn-only，不影响退出码），
# 3b 埋点模式为既有沉睡行为（warn-only）——两者均不进断言
PROJECT_DIR="__REPO_ROOT__"
SPEC_FILE="docs/spec.md"
CHANGE_IMPACT_FILE="docs/plan.md"
