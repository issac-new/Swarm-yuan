# shift-left fixture (violating)：spec 缺 §19 测试左移段（fail）+ plan 缺 §20 变更左移段（fail）
# + spec 无回滚预案声明（fail）；§21 缺失仅 warn；exit 非 0
PROJECT_DIR="__REPO_ROOT__"
SPEC_FILE="docs/spec.md"
CHANGE_IMPACT_FILE="docs/plan.md"
