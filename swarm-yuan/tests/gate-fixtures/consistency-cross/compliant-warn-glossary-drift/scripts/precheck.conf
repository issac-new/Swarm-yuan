# consistency-cross fixture (compliant-warn-glossary-drift)：术语表含代码中不存在的标识符（命名漂移）
# 现语义 warn-only：标识符未命中仅触发 warn「在代码中未找到」，退出码仍为 0——
# 按 sensitive/compliant 先例以 expect-output 断言 warn 文案，目录名以 compliant 开头匹配退出码期望。
PROJECT_DIR="__REPO_ROOT__"
WRITABLE_DIRS=("src")
GLOSSARY_FILE="docs/glossary.md"
SOR_FILE="docs/sor.md"
