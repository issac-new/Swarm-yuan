# state fixture（违例气味样本 → 现行行为 warn-only，退出 0）：
# check_state 现行实现不含 fail 路径（巨型 store / prop drilling / 派生 useState 均只 warn 披露），
# 本 fixture 以 expected-ids 固定三条 warn 行 + 退出码 0，如实登记「violating 语义 = warn 断言」，
# 不命名为 violating*（runner 对 violating* 期望退出非 0，与该门禁现行语义冲突）。
PROJECT_DIR="__REPO_ROOT__"
STORE_DIR="src/store"
MAX_STORE_LINES=100
COMPONENT_DIR="src/components"
