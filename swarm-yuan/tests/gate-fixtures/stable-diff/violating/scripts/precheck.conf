# stable-diff fixture (violating)：git 仓库内改稳定层 src/stable/order.ts，但项目无任何 spec 声明 MODIFIED → fail
# .git 由 scripts/setup.sh 运行时初始化（不入库），teardown.sh 还原工作区并移除 .git
PROJECT_DIR="__REPO_ROOT__"
STABLE_GLOBS=("src/stable/**")
