# stable-diff fixture (compliant)：同样改稳定层 src/stable/order.ts，但 specs/ 下 spec 已声明 MODIFIED → pass
# .git 由 scripts/setup.sh 运行时初始化（不入库），teardown.sh 还原工作区并移除 .git
PROJECT_DIR="__REPO_ROOT__"
STABLE_GLOBS=("src/stable/**")
# 供 spec 兜底检索（grep -rliE '复用约束|拼装合规声明|MODIFIED'）定位 specs/ 下的变更 spec
WRITABLE_DIRS=("specs")
