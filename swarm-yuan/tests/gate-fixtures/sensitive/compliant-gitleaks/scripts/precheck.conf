# sensitive fixture (compliant-gitleaks)：mock gitleaks 工具路径无命中 → pass（报告空数组）
# PATH 注入走 conf；setup.sh 生成 bin/gitleaks mock（同 violating-gitleaks 模式）。
PROJECT_DIR="__REPO_ROOT__"
export PATH="__REPO_ROOT__/bin:$PATH"
SENSITIVE_TOOL="auto"
SCAN_DIRS=("src")
