# sbom fixture (violating)：node_modules 内 GPL-3.0 组件 + 块名单命中 → fail
# node_modules 被仓库 .gitignore 排除，由 scripts/setup.sh 运行时生成、teardown.sh 清理。
PROJECT_DIR="__REPO_ROOT__"
SBOM_REQUIRED=1
SBOM_OUTPUT_DIR="__REPO_ROOT__/.sbom-out"
SBOM_LICENSE_BLOCKLIST=("GPL-3.0")
