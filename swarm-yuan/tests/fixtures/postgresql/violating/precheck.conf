# precheck.conf — postgresql fixture (violating: max(id)+1 取号 + 无 WHERE 全表 DELETE + json 无 GIN + 数据源无连接池 → fw_pgsql_pk_identity / fw_pgsql_dml_where 双 fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/postgresql/violating"
ACTIVE_FRAMEWORKS=("postgresql")
PGSQL_SQL_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/postgresql/violating/**/*.sql" "__REPO_ROOT__/swarm-yuan/tests/fixtures/postgresql/violating/**/*.yml")
PGSQL_SCHEMA_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/postgresql/violating/**/schema.sql")
