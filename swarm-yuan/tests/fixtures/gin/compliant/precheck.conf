# precheck.conf — gin fixture (compliant: ShouldBind + binding 标签 + c.Copy + Default(Recovery) + Shutdown + Abort+return → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/gin/compliant"
ACTIVE_FRAMEWORKS=("gin")
GIN_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/gin/compliant/**/*.go")
