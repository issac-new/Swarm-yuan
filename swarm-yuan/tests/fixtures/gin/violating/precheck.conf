# precheck.conf — gin fixture (violating: gin.New 无 Recovery + c.Bind + goroutine 用 c 非 c.Copy → context_copy fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/gin/violating"
ACTIVE_FRAMEWORKS=("gin")
GIN_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/gin/violating/**/*.go")
