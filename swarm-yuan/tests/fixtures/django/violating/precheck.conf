# precheck.conf — django fixture (violating: SECRET_KEY 硬编码 + DEBUG=True + f-string 拼接 SQL → secret_key/debug/raw_sql fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/django/violating"
ACTIVE_FRAMEWORKS=("django")
DJANGO_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/django/violating/**/*.py")
