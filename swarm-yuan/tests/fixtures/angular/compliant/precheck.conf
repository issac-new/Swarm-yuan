# precheck.conf — angular fixture (compliant: standalone + signal + takeUntilDestroyed + OnPush + loadComponent → 全 pass）
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/angular/compliant"
ACTIVE_FRAMEWORKS=("angular")
ANGULAR_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/angular/compliant/**/*.ts")
