# precheck.conf — validation fixture (violating: String @NotNull + ConstraintValidator 可变实例字段 → fail)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/validation/violating"
ACTIVE_FRAMEWORKS=("validation")
VALIDATION_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/validation/violating/**/*.java")
