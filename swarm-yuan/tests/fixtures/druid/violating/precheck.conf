# precheck.conf — druid fixture (violating: StatViewServlet 无鉴权 → fw_druid_statview_expose fail 主触发；缺 wall filter → warn；缺 max-active → warn；slow-sql-millis 未配套 log-slow-sql → warn)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/druid/violating"
ACTIVE_FRAMEWORKS=("druid")
DRUID_CONFIG_FILES=("__REPO_ROOT__/swarm-yuan/tests/fixtures/druid/violating/application.yml")
