# precheck.conf — druid fixture (compliant: StatViewServlet 有鉴权 + wall filter 启用 + 连接池参数齐 + slow-sql 配套)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/druid/compliant"
ACTIVE_FRAMEWORKS=("druid")
DRUID_CONFIG_FILES=("__REPO_ROOT__/swarm-yuan/tests/fixtures/druid/compliant/application.yml")
