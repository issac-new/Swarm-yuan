# precheck.conf — fastify fixture (compliant: schema+response / setErrorHandler / logger / fp 插件 / cors 白名单 / 限流 / 认证 / swagger → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/fastify/compliant"
ACTIVE_FRAMEWORKS=("fastify")
FASTIFY_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/fastify/compliant/**/*.js" "__REPO_ROOT__/swarm-yuan/tests/fixtures/fastify/compliant/**/package.json")
