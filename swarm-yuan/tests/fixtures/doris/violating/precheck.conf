# precheck.conf — doris fixture (violating: 无分桶 + rep=3 单节点 + GROUP BY 无 ROLLUP + JOIN 无 COLOCATE + MV 无 REFRESH + 超宽 VARCHAR + WHERE 无索引 + 无动态分区 + StreamLoad + 无 RG)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/doris/violating"
ACTIVE_FRAMEWORKS=("doris")
DORIS_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/doris/violating/schema.sql")
