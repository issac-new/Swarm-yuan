# precheck.conf — doris fixture (compliant: 分桶 + rep=1 + ROLLUP + COLOCATE + MV REFRESH + 合理 VARCHAR + 动态分区 + query_timeout)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/doris/compliant"
ACTIVE_FRAMEWORKS=("doris")
DORIS_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/doris/compliant/schema.sql")
