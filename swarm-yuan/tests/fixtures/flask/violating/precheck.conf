# precheck.conf — flask fixture (violating: SECRET_KEY 硬编码 + debug=True + 明文 DB URI + 无错误处理 + 蓝图循环导入 → secret_key/debug/db_credentials fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/flask/violating"
ACTIVE_FRAMEWORKS=("flask")
FLASK_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/flask/violating/**/*.py")
