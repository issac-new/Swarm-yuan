# precheck.conf — opentelemetry fixture (compliant: service.name + OTLP exporter + sampler + Baggage + metrics + logs + shutdown → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/opentelemetry/compliant"
ACTIVE_FRAMEWORKS=("opentelemetry")
OPENTELEMETRY_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/opentelemetry/compliant/**/*.ts")
