server:
  port: 8080
spring:
  profiles:
    active: prod
  main:
    banner-mode: log
  datasource:
    url: jdbc:mysql://localhost:3306/app
    hikari:
      maximum-pool-size: 20
management:
  endpoints:
    web:
      exposure:
        include: health,info
