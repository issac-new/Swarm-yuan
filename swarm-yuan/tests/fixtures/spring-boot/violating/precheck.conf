# precheck.conf — spring-boot fixture (violating: @Transactional 同类自调用 + Actuator exposure.include=* + javax import → fail)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/spring-boot/violating"
ACTIVE_FRAMEWORKS=("spring-boot")
SPRINGBOOT_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/spring-boot/violating/**/*.java")
SPRINGBOOT_CONFIG_FILES=("__REPO_ROOT__/swarm-yuan/tests/fixtures/spring-boot/violating/application.yml")
