# precheck.conf — langchain fixture (compliant: LCEL + .invoke/ainvoke + 环境变量密钥 + timeout/max_retries + 迭代/token 上限 → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/langchain/compliant"
ACTIVE_FRAMEWORKS=("langchain")
LANGCHAIN_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/langchain/compliant/**/*.py")
