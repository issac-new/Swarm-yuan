# precheck.conf — junit5-mockito fixture (violating: 仅 assertNotNull 断言 → fail；@MockBean 未清理 → warn)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/junit5-mockito/violating"
ACTIVE_FRAMEWORKS=("junit5-mockito")
JUNIT_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/junit5-mockito/violating/**/*.java")
