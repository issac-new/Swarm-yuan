# precheck.conf — kubernetes fixture (compliant: digest + runAsNonRoot + 无 privileged + resources + probes + namespace + NetworkPolicy + PDB → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/kubernetes/compliant"
ACTIVE_FRAMEWORKS=("kubernetes")
KUBERNETES_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/kubernetes/compliant/**/*.yaml" "__REPO_ROOT__/swarm-yuan/tests/fixtures/kubernetes/compliant/**/*.yml")
