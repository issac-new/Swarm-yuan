# precheck.conf — kubernetes fixture (violating: latest + privileged + 无 runAsNonRoot + Secret 明文 → 4 fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/kubernetes/violating"
ACTIVE_FRAMEWORKS=("kubernetes")
KUBERNETES_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/kubernetes/violating/**/*.yaml" "__REPO_ROOT__/swarm-yuan/tests/fixtures/kubernetes/violating/**/*.yml")
