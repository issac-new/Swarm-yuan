# precheck.conf — tdengine fixture (violating: 多设备表无超级表 + 首列 BIGINT + USING 无 TAGS + 标签>8 + 无 KEEP + 无分离 + 无 DAYS/ROWS + 原生 JDBC + 单条 INSERT + WHERE)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/tdengine/violating"
ACTIVE_FRAMEWORKS=("tdengine")
TDENGINE_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/tdengine/violating/schema.sql")
