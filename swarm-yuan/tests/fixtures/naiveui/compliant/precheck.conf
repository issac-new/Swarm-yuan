# precheck.conf — naiveui fixture (compliant: 具名导入 + rules 校验 + before-upload 大小校验 → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/naiveui/compliant"
ACTIVE_FRAMEWORKS=("naiveui")
NAIVEUI_FILE_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/naiveui/compliant/**/*.vue")
