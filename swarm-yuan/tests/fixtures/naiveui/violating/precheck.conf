# precheck.conf — naiveui fixture (violating: 全局注册 + createDiscreteApi + n-upload 无大小校验 → no_global_register + upload_size_limit fail 主触发；main.ts 混用 element-plus → no_dual_ui fail）
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/naiveui/violating"
ACTIVE_FRAMEWORKS=("naiveui")
NAIVEUI_FILE_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/naiveui/violating/**/*.vue" "__REPO_ROOT__/swarm-yuan/tests/fixtures/naiveui/violating/**/*.ts")
