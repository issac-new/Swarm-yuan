# precheck.conf — element fixture (violating: 全量 import + 手动表单校验 + 硬编码中文 + el-upload 无大小限制 → fw_element_upload_size_limit fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/element/violating"
ACTIVE_FRAMEWORKS=("element")
ELEMENT_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/element/violating/**/*.vue")
