# precheck.conf — element fixture (compliant: 按需引入 + rules 校验 + i18n + destroy-on-close + before-upload 大小校验 → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/element/compliant"
ACTIVE_FRAMEWORKS=("element")
ELEMENT_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/element/compliant/**/*.vue")
