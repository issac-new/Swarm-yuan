# precheck.conf — spark fixture (compliant: shuffle 分区已配 + collect 仅采样 + 聚合打散 + 广播 + persist)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/spark/compliant"
ACTIVE_FRAMEWORKS=("spark")
SPARK_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/spark/compliant/Job.scala")
