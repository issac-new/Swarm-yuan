# precheck.conf — spark fixture (violating: .collect() OOM + 数据倾斜 + join 无广播 + 多 action 无 persist + 迭代无 checkpoint + window 无 watermark)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/spark/violating"
ACTIVE_FRAMEWORKS=("spark")
SPARK_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/spark/violating/Job.scala")
