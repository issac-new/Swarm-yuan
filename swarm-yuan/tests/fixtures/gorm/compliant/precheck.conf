# precheck.conf — gorm fixture (compliant: Preload + SetMaxOpenConns + AutoMigrate 仅测试 + ErrRecordNotFound → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/gorm/compliant"
ACTIVE_FRAMEWORKS=("gorm")
GORM_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/gorm/compliant/**/*.go")
