# precheck.conf — gorm fixture (violating: for-range 内 Find 无 Preload + gorm.Open 无 SetMaxOpenConns + AutoMigrate 在 main.go → n_plus_one/conn_pool fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/gorm/violating"
ACTIVE_FRAMEWORKS=("gorm")
GORM_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/gorm/violating/**/*.go")
