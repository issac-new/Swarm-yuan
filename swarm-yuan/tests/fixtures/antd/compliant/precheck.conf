# precheck.conf — antd fixture (compliant: App.useApp 注入式 + useForm + beforeUpload 大小校验 → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/antd/compliant"
ACTIVE_FRAMEWORKS=("antd")
ANTD_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/antd/compliant/**/*.tsx")
