# precheck.conf — antd fixture (violating: 静态 message 调用 + Form.create + Upload 无 beforeUpload → fw_antd_app_useapp + fw_antd_upload_size_limit fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/antd/violating"
ACTIVE_FRAMEWORKS=("antd")
ANTD_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/antd/violating/**/*.tsx")
