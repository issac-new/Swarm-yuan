# precheck.conf — mybatis fixture (compliant: #{col} 参数化，全门禁 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/mybatis/compliant"
ACTIVE_FRAMEWORKS=("mybatis")
MYBATIS_MAPPER_DIRS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/mybatis/compliant")
MYBATIS_SRC_GLOBS=()
SQL_INJECTION_WHITELIST=("ORDER BY \${orderBy}")
