<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 漏改字段场景：实体 User.status 已改名 orderState，本 resultMap 的 property="status" 失同步
     → fw_mybatis_field_sync(fail)；property="userName" 同步正常（对照组，不应报） -->
<mapper namespace="com.example.FieldSyncMapper">
  <resultMap id="userMap" type="com.example.User">
    <id column="id" property="id"/>
    <result column="user_name" property="userName"/>
    <result column="status" property="status"/>
  </resultMap>
  <select id="listSync" resultMap="userMap">
    SELECT id, user_name, status FROM t_user
  </select>
</mapper>
