# precheck.conf — mybatis fixture (violating: ORDER BY ${col} 未白名单 + Mapper 接口数≠XML namespace 数 + resultType/resultMap 并存
#   → fw_mybatis_dollar / fw_mybatis_binding / fw_mybatis_select_dup_result 三 fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/mybatis/violating"
ACTIVE_FRAMEWORKS=("mybatis")
MYBATIS_MAPPER_DIRS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/mybatis/violating")
MYBATIS_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/mybatis/violating/**/*.java")
SQL_INJECTION_WHITELIST=("ORDER BY \${orderBy}")
