# precheck.conf — dockerfile fixture (compliant: digest + USER app + 多阶段 + HEALTHCHECK + .dockerignore + apt 清理 + COPY --chown → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/dockerfile/compliant"
ACTIVE_FRAMEWORKS=("dockerfile")
DOCKERFILE_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/dockerfile/compliant/Dockerfile" "__REPO_ROOT__/swarm-yuan/tests/fixtures/dockerfile/compliant/.dockerignore")
