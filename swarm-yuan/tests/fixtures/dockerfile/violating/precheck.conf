# precheck.conf — dockerfile fixture (violating: :latest + 无 USER + ENV PASSWORD + 无 HEALTHCHECK → 3 fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/dockerfile/violating"
ACTIVE_FRAMEWORKS=("dockerfile")
DOCKERFILE_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/dockerfile/violating/Dockerfile" "__REPO_ROOT__/swarm-yuan/tests/fixtures/dockerfile/violating/.dockerignore")
