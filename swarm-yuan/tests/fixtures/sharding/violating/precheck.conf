# precheck.conf — sharding fixture (violating: update t_order 缺 user_id 分片键 → fw_sharding_key_in_dml fail；直写广播表 t_dict → fw_sharding_broadcast_write fail)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/sharding/violating"
ACTIVE_FRAMEWORKS=("sharding")
SHARDED_TABLES=("t_order" "t_order_item")
SHARDING_KEY_COLUMNS=("t_order=user_id" "t_order_item=order_id")
SHARDING_BROADCAST_TABLES=("t_dict")
MYBATIS_MAPPER_DIRS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/sharding/violating")
