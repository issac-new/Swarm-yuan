dubbo.application.name=order-provider
dubbo.registry.address=nacos://nacos:8848
# qos 端口公网暴露 —— fw_dubbo_qos_exposure(fail) 主触发
dubbo.qos.accept.foreign.ip=true
# mock=force 屏蔽真实调用 —— fw_dubbo_mock_degrade(fail) 触发（P1-1 唤醒）
dubbo.reference.com.example.OrderService.mock=force:return null
