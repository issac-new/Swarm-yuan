[mysqld]
# compliant: 死锁检测开 + autocommit=1 + 慢查询日志 + 显式 RC 隔离级别
innodb_deadlock_detect=ON
autocommit=1
transaction-isolation=READ-COMMITTED
slow_query_log=1
long_query_time=1
log_queries_not_using_indexes=1
