# precheck.conf — fastapi fixture (compliant: Pydantic v2 + APIRouter + response_model + lifespan + 认证 → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/fastapi/compliant"
ACTIVE_FRAMEWORKS=("fastapi")
FASTAPI_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/fastapi/compliant/**/*.py")
