# precheck.conf — elasticsearch fixture (compliant: search_after + filter 上下文精确查询 → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/elasticsearch/compliant"
ACTIVE_FRAMEWORKS=("elasticsearch")
ES_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/elasticsearch/compliant/**/*.java")
