# precheck.conf — sqlalchemy fixture (compliant: 2.x select + 加载策略 + 池回收 + 批量插入 + Alembic → 全 pass)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/sqlalchemy/compliant"
ACTIVE_FRAMEWORKS=("sqlalchemy")
SQLALCHEMY_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/sqlalchemy/compliant/**/*.py")
