{
    "name": "acme/shop-api",
    "description": "php fixture compliant: composer.json 与 composer.lock 依赖一致",
    "require": {
        "php": "^8.2",
        "monolog/monolog": "^3.5",
        "guzzlehttp/guzzle": "^7.8"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "scripts": {
        "test": "phpunit"
    }
}
