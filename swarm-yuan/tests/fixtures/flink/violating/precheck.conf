# precheck.conf — flink fixture (violating: 无 checkpoint + 无 watermark + 无 restart strategy + 无 uid → fw_flink_checkpoint_enabled fail 主触发)
PROJECT_DIR="__REPO_ROOT__/swarm-yuan/tests/fixtures/flink/violating"
ACTIVE_FRAMEWORKS=("flink")
FLINK_SRC_GLOBS=("__REPO_ROOT__/swarm-yuan/tests/fixtures/flink/violating/**/*.java")
