# offline-cache 治理说明（v2026.07.20 起）：
# - 二进制依赖（*.whl / *.tgz）已迁移到 GitHub Release 附件（v2026.07.20-offline tag），
#   install-offline-win.sh 自动从 Release 下载，不再纳入 git 跟踪。
# - gstack/ superpowers/ 是 clone 的第三方仓库（gitlink 已移除），本地使用需手动 clone。
# - zip 打包临时产物同样忽略。
# - 注意：*.whl/*.tgz 在子目录（graphify-wheels/ npm/），需用 ** 递归匹配。
offline-cache/**/*.zip
offline-cache/**/*.whl
offline-cache/**/*.tgz
offline-cache/gstack/
offline-cache/superpowers/
